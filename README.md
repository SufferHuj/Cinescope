# Cinescope

Cinescope - это комплексный проект автоматизации тестирования для кинотеатрального сервиса. Проект обеспечивает полное покрытие тестами всех ключевых компонентов системы: от API endpoints до операций с базой данных, включая аутентификацию, управление контентом и платежные операции.

## Архитектура проекта

Проект построен по модульной архитектуре с четким разделением ответственности:

### 🎯 Основные компоненты

- **API Layer** (`api/`) - Менеджеры для взаимодействия с REST API
- **Database Layer** (`db_models/`, `db_requester/`) - Модели данных и модульная работа с БД
- **Test Suites** (`tests/`) - Организованные тестовые наборы
- **Utilities** (`utils/`, `custom_requester/`) - Вспомогательные инструменты
- **Configuration** (`resources/`, `constants.py`) - Конфигурация и константы

### 🚀 Ключевые возможности

- **Полное API тестирование**: Покрытие всех REST endpoints сервиса
- **Многоуровневая аутентификация**: Тестирование ролевой модели (USER, ADMIN, SUPER_ADMIN)
- **Управление контентом**: CRUD операции для фильмов, жанров и отзывов
- **Платежная интеграция**: Тестирование обработки платежей с тестовыми картами
- **Database Testing**: Прямое тестирование операций с базой данных через модульные хелперы
- **Модульная архитектура БД**: Разделение DB операций по доменам (users, movies, payments, etc.)
- **Транзакционное тестирование**: Тестирование операций с балансами и переводами между счетами
- **Pydantic валидация**: Автоматическая валидация всех API ответов с типизированными моделями
- **Генерация данных**: Автоматическое создание реалистичных тестовых данных
- **Параллельное выполнение**: Оптимизированное время выполнения тестов

## Технологический стек

### Core Framework
- **Python 3.x** - основной язык разработки
- **pytest 8.3.5** - современный фреймворк тестирования
- **SQLAlchemy** - ORM для работы с базой данных

### HTTP & API Testing  
- **requests 2.32.3** - HTTP клиент для API тестирования
- **pydantic 2.11.7** - валидация и сериализация данных

### Test Data & Utilities
- **Faker 37.6.0** - генерация реалистичных тестовых данных
- **python-dotenv 1.1.0** - управление переменными окружения

### Reporting & Execution
- **pytest-html 4.1.1** - генерация детальных HTML отчетов
- **pytest-xdist 3.6.1** - параллельное выполнение тестов
- **pytest-metadata 3.1.1** - метаданные для отчетов

## 📁 Структура проекта

```
Cinescope/
├── 🔧 Конфигурация
│   ├── conftest.py           # Глобальные фикстуры и настройки pytest
│   ├── constants.py          # Константы проекта (URL, роли, статусы)
│   ├── pytest.ini           # Конфигурация pytest с маркерами
│   └── requirement.txt       # Зависимости проекта
│
├── 🌐 API Layer
│   ├── api_manager.py        # Базовый менеджер для всех API операций
│   ├── auth_api.py          # Аутентификация и авторизация
│   ├── genres_api.py        # Управление жанрами фильмов
│   ├── movies_api.py        # CRUD операции с фильмами
│   ├── payment_api.py       # Обработка платежных операций
│   ├── reviews_api.py       # Система отзывов и рейтингов
│   └── user_api.py          # Управление пользователями
│
├── 🗄️ Database Layer
│   ├── db_models/           # ORM модели
│   │   ├── db_account_transaction_template_model.py  # Модель шаблона транзакций счетов
│   │   ├── db_genre_model.py    # Модель жанра фильма
│   │   ├── db_movie_model.py    # Модель фильма с жанрами
│   │   ├── db_payment_model.py  # Модель платежа за фильм
│   │   ├── db_review_model.py   # Модель отзыва к фильму
│   │   └── db_user_model.py     # Модель пользователя с ролями
│   └── db_requester/        # Работа с БД
│       ├── db_client.py         # Подключение к базе данных
│       ├── db_helpers.py        # Главный класс-координатор для всех DB операций
│       └── db_helpers_modules/  # Модульные хелперы по доменам
│           ├── __init__.py          # Экспорт всех модульных хелперов
│           ├── base_db_helper.py    # Базовый класс с общими методами
│           ├── user_db_helper.py    # Операции с пользователями
│           ├── movie_db_helper.py   # Операции с фильмами
│           ├── genre_db_helper.py   # Операции с жанрами
│           ├── account_db_helper.py # Операции с аккаунтами и транзакциями
│           ├── review_db_helper.py  # Операции с отзывами
│           └── payment_db_helper.py # Операции с платежами

├── 📊 Data Models & Entities
│   ├── entities/            # Бизнес-сущности
│   │   └── user.py             # Пользовательская сущность
│   └── models/              # Pydantic модели
│       ├── auth_model.py       # Модели аутентификации
│       ├── genre_model.py      # Модели жанров фильмов
│       ├── movie_model.py      # Модели фильмов и операций с ними
│       ├── payment_model.py    # Модели платежной системы
│       ├── review_model.py     # Модели отзывов к фильмам
│       └── user_model.py       # Пользовательские модели
│
├── 🧪 Test Suites
│   ├── api/                 # API тестирование
│   │   ├── test_auth_api.py        # Тесты аутентификации
│   │   ├── test_genres_api.py      # Тесты API жанров
│   │   ├── test_movies_api.py      # Тесты API фильмов
│   │   ├── test_payment_api.py     # Тесты платежной системы
│   │   ├── test_reviews_api.py     # Тесты системы отзывов
│   │   └── test_user_api.py        # Тесты пользовательского API
│   ├── db/                  # Database тестирование
│   │   ├── test_db_accounts_transaction_template.py  # Тесты транзакций и балансов
│   │   ├── test_db_genres.py       # Тесты операций с БД жанров
│   │   ├── test_db_movies.py       # Тесты операций с БД фильмов
│   │   ├── test_db_payments.py     # Тесты операций с БД платежей
│   │   ├── test_db_reviews.py      # Тесты операций с БД отзывов
│   │   └── test_db_users.py        # Тесты операций с БД пользователей
│   └── ui/                  # UI тестирование
│       ├── elements/        
│       │   ├── test_buttons.py               # Сценарии кликов и состояний
│       │   ├── test_checkbox.py              # Проверка дерева чекбоксов
│       │   ├── test_dynamic_properties.py    # Появление/доступность элементов
│       │   ├── test_links.py                 # Проверка ссылок
│       │   ├── test_radio_button.py          # Радиокнопки
│       │   ├── test_text_box.py              # Текстовые поля
│       │   ├── test_upload_download.py       # Загрузка/скачивание
│       │   └── test_webtables.py             # Таблицы
│       ├── forms/
│       │   └── test_registration_form.py     # Форма регистрации
│       ├── widgets/
│       │   └── test_slider.py                # Слайдер
│       ├── test_cs_registration.py           # Регистрация через UI
│       └── test_login_page.py                # Тесты страницы входа
│
├── 🔧 Test Fixtures
│   ├── __init__.py          # Инициализация пакета фикстур
│   ├── base_fixtures.py     # Базовые фикстуры (HTTP сессии, API менеджеры)
│   ├── auth_fixtures.py     # Фикстуры аутентификации (пользователи, роли)
│   ├── db_fixtures.py       # Фикстуры для работы с БД (сессии, хелперы)
│   ├── genres_fixtures.py   # Фикстуры для тестирования жанров
│   ├── movies_fixtures.py   # Фикстуры для создания и управления фильмами
│   ├── payment_fixtures.py  # Фикстуры для тестирования платежной системы
│   └── reviews_fixtures.py  # Фикстуры для тестирования системы отзывов
│
├── 🛠️ Utilities & Helpers
│   ├── custom_requester/    # HTTP клиент
│   │   └── custom_requester.py     # Кастомизированный HTTP requester
│   └── utils/               # Утилиты
│       ├── data_generator.py       # Генерация тестовых данных
│       └── tools.py                # Класс Tools: пути артефактов, метки времени
│
└── 📋 Resources & Configuration
    ├── db_creds.py          # Конфигурация подключения к БД
    ├── test_card_data.py    # Тестовые данные банковских карт
    └── user_creds.py        # Учетные данные тестовых пользователей
```

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.8+
- pip (менеджер пакетов Python)
- Доступ к тестовой среде API
- Подключение к тестовой базе данных

### Установка и настройка

1. **Клонирование репозитория**
   ```bash
   git clone <repository-url>
   cd Cinescope
   ```

2. **Создание виртуального окружения**
   ```bash
   python -m venv venv
   
   # Windows
   venv\Scripts\activate
   
   # Linux/macOS
   source venv/bin/activate
   ```

3. **Установка зависимостей**
   ```bash
   pip install -r requirement.txt
   ```

4. **Настройка переменных окружения**
   - Создайте файл `.env` в корне проекта
   - Добавьте необходимые переменные (URL API, данные БД)

### Запуск тестов

#### Все тесты
```bash
pytest
```

#### Тесты по категориям
```bash
# API тесты
pytest tests/api/ -v

# Тесты базы данных  
pytest tests/db/ -v

# Smoke тесты (быстрые)
pytest -m smoke

# Регрессионные тесты
pytest -m regression
```

#### Параллельное выполнение
```bash
# Запуск в 4 потока
pytest -n 4

# Автоматическое определение количества процессоров
pytest -n auto
```

#### Генерация отчетов
```bash
# HTML отчет
pytest --html=reports/report.html --self-contained-html

# Подробный вывод с логами
pytest -v -s --tb=short
```

## 🏗️ Архитектура тестирования

### Слои тестирования

#### 1. **API Layer Testing** 
- **Scope**: REST API endpoints тестирование
- **Coverage**: Аутентификация, CRUD операции, бизнес-логика
- **Tools**: requests, pydantic для валидации
- **Patterns**: Page Object Model для API

#### 2. **Database Testing**
- **Scope**: Прямое тестирование операций с базой данных через модульные хелперы
- **Coverage**: CRUD операции, транзакции, целостность данных
- **Tools**: SQLAlchemy ORM, модульные DB хелперы по доменам
- **Patterns**: Repository pattern, Domain-driven design, Database fixtures
- **Architecture**: Модульная структура с разделением по доменам (users, movies, payments, reviews, genres, accounts)

#### 2.1. **Transaction Testing**
- **Account Balance Operations**: Тестирование операций с балансами счетов
- **Multi-Account Transfers**: Проверка переводов между несколькими счетами
- **Transaction Templates**: Использование шаблонов для стандартных операций
- **Data Integrity**: Контроль целостности данных при транзакциях
- **Error Handling**: Обработка ошибок и откат транзакций

#### 3. **Integration Testing**
- **Scope**: Взаимодействие между компонентами системы
- **Coverage**: API ↔ Database, Authentication flow, Business workflows
- **Tools**: Комбинация API и DB тестов
- **Patterns**: End-to-end scenarios

### Модульная архитектура Database Layer

#### Структура DB хелперов
- **DBHelper** - главный координатор, предоставляющий доступ ко всем доменам хелперам
- **BaseDBHelper** - базовый класс с общими методами (cleanup, count operations)
- **Доменные хелперы**:
  - `UserDBHelper` - операции с пользователями
  - `MovieDBHelper` - операции с фильмами  
  - `GenreDBHelper` - операции с жанрами
  - `PaymentDBHelper` - операции с платежами
  - `ReviewDBHelper` - операции с отзывами
  - `AccountDBHelper` - операции с аккаунтами и транзакциями

#### Преимущества модульного подхода
- **Разделение ответственности**: каждый хелпер отвечает за свой домен
- **Легкость поддержки**: изменения в одном домене не влияют на другие
- **Переиспользование**: общие методы вынесены в базовый класс
- **Читаемость тестов**: `db_helper.users.create_test_user()` вместо `db_helper.create_test_user()`

### Управление тестовыми данными

#### Генерация данных
- **Faker Integration**: Реалистичные тестовые данные
- **Custom Generators**: Специализированные генераторы для доменных объектов
- **Data Templates**: Предопределенные шаблоны для различных сценариев

#### Изоляция тестов
- **Database Transactions**: Откат изменений после каждого теста
- **Test Fixtures**: Подготовка и очистка тестового окружения
- **Parallel Execution**: Безопасное параллельное выполнение

### Система ролей и авторизации

```python
# Поддерживаемые роли
USER         # Базовые операции (просмотр, отзывы)
ADMIN        # Управление контентом (фильмы, жанры)
SUPER_ADMIN  # Полный доступ (пользователи, система)
```

### Маркеры тестов

- `@pytest.mark.smoke` - Быстрые критически важные тесты
- `@pytest.mark.regression` - Полный набор регрессионных тестов  
- `@pytest.mark.slow` - Длительные тесты (интеграционные)
- `@pytest.mark.api` - API-специфичные тесты
- `@pytest.mark.negative` - Тесты негативных сценариев

## 💳 Платежная система

### Тестовые данные карт

Проект включает предопределенные тестовые данные карт в файле `resources/test_card_data.py`:

- Номер карты: `***` (Visa Test Card)
- Срок действия: `12/25`
- CVV: `***`
- Держатель карты: `Test User`

### Особенности тестирования платежей

- ✅ Позитивные сценарии: успешное создание платежа
- ❌ Негативные сценарии: невалидные карты, неавторизованный доступ, несуществующие фильмы
- 🔄 Автоматическое пропускание тестов при недоступности внешнего сервиса карт (503 ошибка)
- 📊 Динамическая генерация сумм платежей для каждого теста

### Интеграция с CI/CD

Проект готов для интеграции с системами непрерывной интеграции:

- **GitHub Actions** - автоматический запуск тестов
- **Jenkins** - интеграция в корпоративные пайплайны  
- **Docker** - контейнеризация тестового окружения
- **Allure** - расширенная отчетность (при необходимости)

## 📊 Отчеты и логирование

Тесты можно запускать с генерацией отчетов:

```bash
# HTML отчет
.venv\Scripts\python.exe -m pytest --html=reports/report.html

# JUnit XML отчет
.venv\Scripts\python.exe -m pytest --junitxml=reports/junit.xml

# Подробный вывод
.venv\Scripts\python.exe -m pytest -v

# Показать локальные переменные при ошибках
.venv\Scripts\python.exe -m pytest -l
```

## 🤝 Участие в разработке

### Требования к коду

- **Python Code Style**: Следование PEP 8 стандартам
- **Docstrings**: Обязательное документирование всех функций и классов
- **Type Hints**: Использование аннотаций типов для лучшей читаемости
- **Error Handling**: Корректная обработка исключений и граничных случаев

### Структура коммитов

```
feat: добавление новой функциональности
fix: исправление ошибок
test: добавление или изменение тестов
docs: обновление документации
refactor: рефакторинг кода без изменения функциональности
style: форматирование кода
```

### Процесс разработки

1. **Fork** репозитория
2. **Создание feature branch** из main
3. **Разработка** с покрытием тестами
4. **Локальное тестирование** всех изменений
5. **Pull Request** с подробным описанием изменений

### Добавление новых тестов

При добавлении новых тестов следуйте структуре:

```python
class TestNewFeature:
    """
    Тесты для новой функциональности.
    
    Включает проверку:
    - Позитивных сценариев
    - Негативных сценариев  
    - Граничных случаев
    """
    
    def test_positive_scenario(self, fixture_name):
        """Тест успешного выполнения операции."""
        # Arrange
        # Act  
        # Assert
        
    def test_negative_scenario(self, fixture_name):
        """Тест обработки ошибочных данных."""
        # Arrange
        # Act
        # Assert
```

## 📝 Стандарты кодирования

### Именование

- **Классы**: PascalCase (`TestMovieAPI`)
- **Методы**: snake_case (`test_create_movie`)
- **Константы**: UPPER_SNAKE_CASE (`BASE_URL`)
- **Переменные**: snake_case (`user_data`)

### Организация импортов

```python
# Стандартные библиотеки
import os
import sys
from datetime import datetime

# Сторонние библиотеки
import pytest
import requests
from faker import Faker

# Локальные импорты
from api.api_manager import ApiManager
from utils.data_generator import DataGenerator
```

### Документирование

Все публичные методы должны содержать docstring с описанием:

```python
def create_payment(self, movie_id: int, card_data: dict) -> dict:
    """
    Создание платежа за фильм.
    
    Args:
        movie_id (int): Идентификатор фильма
        card_data (dict): Данные банковской карты
        
    Returns:
        dict: Ответ API с информацией о платеже
        
    Raises:
        PaymentError: При ошибке обработки платежа
    """
```

---

<div align="center">

### 🎬 Cinescope Test Automation Framework

*Создано с ❤️ к автоматизации тестирования*

[![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)](https://python.org)
[![Pytest](https://img.shields.io/badge/Pytest-8.3.5-green.svg)](https://pytest.org)
[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

**🚀 Качественное тестирование • 🔧 Надежная автоматизация • 📊 Детальная отчетность**

</div>